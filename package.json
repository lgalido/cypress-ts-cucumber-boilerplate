{
  "name": "cypress-ts-cucumber-boilerplate",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "fixtures": "node scripts/sync-fixtures.js",
    "cy:open": "npm run fixtures && cypress open",
    "cy:run": "npm run fixtures && cypress run",
    "test": "npm run cy:run"
  },
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^21.0.2",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.3",
    "@types/node": "^22.7.5",
    "cypress": "^13.15.0",
    "cypress-mochawesome-reporter": "^3.8.2",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.670.0"
  },
  "cypress-cucumber-preprocessor": {
    "stepDefinitions": "cypress/e2e/step_definitions/**/*.{js,ts}"
  }
}